CREATE TABLE users;
CREATE TABLE users (
    id int PRIMARY KEY,
    username varchar(255),
    password varchar(255)
    );

Post:
INSERT INTO table (props) VALUES (values);

put:
UPDATE table
SET prop = value
WHERE id = value;

delete:
DELETE FROM table WHERE props;
